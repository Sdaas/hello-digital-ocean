# temp / scratch
*.tmp
.DS_Store

# in-flight SDLC session checkpoint (machine-local; see sdlc-common §5)
SESSION_STATE.md

# local env
.env

# digital-ocean setup (F4) — local config + credentials (machine-local, never commit)
.do/

# demo app (Python) — local runtime data + tool caches
demo/.localdata/
__pycache__/
*.pyc
.pytest_cache/
.venv/
demo/.venv/
